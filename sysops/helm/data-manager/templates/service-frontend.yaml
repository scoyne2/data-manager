apiVersion: v1
kind: Service
metadata:
  name: data-manager-{{ .Values.frontend.serviceName }}
  labels:
    {{- include "data-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.frontend.service.type }}
  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "data-manager.selectorLabels" . | nindent 4 }}

